[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
agent-of-empires = { path = ".." }
clap = { version = "4.5", features = ["derive"] }
clap-markdown = "0.1"
regex = "1"
